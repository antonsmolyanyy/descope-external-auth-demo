[project]
name = "demo-external-auth-mcp"
version = "0.1.0"
description = "Descope-protected FastMCP demo server"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.117.1",
    "fastmcp>=3.4.2",
]

[tool.vercel]
entrypoint = "server:app"
